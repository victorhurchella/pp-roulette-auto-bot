namespace SettingsNS {
  export let isReadyToBet: boolean;
  export let betResult: string | null;
  export let repeatedRow: string | null;
  export let betInRows: Array<string>;
  export const betHistory: number[] = [];
}

export default SettingsNS;
